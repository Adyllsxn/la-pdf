namespace LaPDF.Web.Models.Enums;

public enum CompressionQuality
{
    Low = 0,      // Maior compressão, menor qualidade
    Medium = 1,   // Equilíbrio entre qualidade e tamanho
    High = 2      // Menor compressão, melhor qualidade
}